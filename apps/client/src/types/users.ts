export interface UsersType {
  userId: number;
  grade: number;
  class: number;
  name: string;
  userType: string;
}

export interface MyInfoType {
  userId: number;
  name: string;
  userRole: string;
  hasNewNotification: boolean;
}
